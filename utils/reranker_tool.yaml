tools:
  - class_name: "verl.verl.tools.reranker_tool.RerankerTool"
    config:
      reranker_server_url: "http://localhost:8002/run_code"
      return_full_document: False

    tool_schema:
      type: "function"
      function:
        name: "reranker"
        description: "A tool for searching and reranking documents for a query."
        parameters:
          type: "object"
          properties:
            query:
              type: "string"
              description: "The query to search for."
          required: ["query"]
